# application.properties - Version ASCII uniquement
spring.application.name=backend

server.port=8080
server.address=0.0.0.0

# Database connection
spring.datasource.url=jdbc:postgresql://project_db:5432/app_db
spring.datasource.username=app_user
spring.datasource.password=app_password
spring.datasource.driver-class-name=org.postgresql.Driver

# PostgreSQL ENUMs support
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT configuration
jwt.secret=KJH87f98sd76FSD7f8sd!@#DebateArena#123kldjsf98DSF7
jwt.expiration-ms=900000

# Chatbot configuration
app.chatbot.base-url=http://chatbot:5005
app.chatbot.timeout.connect=5000
app.chatbot.timeout.read=30000
app.chatbot.mode.default=train

# Disable filtering temporarily
spring.resources.filtering=false
